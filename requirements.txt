# Core dependencies
cryptography>=41.0.0
asyncssh>=2.13.0

# Optional dependencies for enhanced features
aiohttp>=3.8.0  # For GitHub API integration
tqdm>=4.64.0  # For progress bars

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
