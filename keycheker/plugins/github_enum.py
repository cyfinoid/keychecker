# github_enum.sh ---
# Check if this key is used in 
    # - GitHub or not? (default)
# Fetch the associated user of the SSH key
# Fetch the associated organizations (public) 
# Bruteforce the repositories
# Clone the repository (if flag is passed to do so)